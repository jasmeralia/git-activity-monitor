services:
  git-activity-monitor:
    image: ghcr.io/jasmeralia/git-activity-monitor:latest
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/data
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
